server:
  port: 9393

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/abhimana06/config-server
            clone-on-start: true